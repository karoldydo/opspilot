# Inline Code Comments Style

1. All inline comments **must** be written entirely in lowercase — no capitalized letters.
2. This applies to every comment syntax used in the repo (`//`, `#`, `/* */`, `<!-- -->`).
3. Do not capitalize the first letter of a comment, even at the beginning of a sentence.

For example: write `// fetch data from api`, not `// Fetch data from API`.
